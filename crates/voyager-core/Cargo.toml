[package]
name = "voyager-core"
version = "0.3.2"
edition = "2021"
description = "Tokenizer and structural parser for Cube Voyager control-statement scripts"
license = "MIT OR Apache-2.0"
readme = "README.md"
repository = "https://github.com/ar-puuk/drut-dev"

# FR-027: zero runtime dependencies. `cargo tree -p voyager-core` must never
# show an external crate here. Do not add one to fix a parsing problem — hand-
# write it instead (see CLAUDE.md / constitution Principle I).
[dependencies]

[dev-dependencies]
